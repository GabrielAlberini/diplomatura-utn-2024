import "./Header.css";

const Header = () => {
  return (
    <header>
      <h1 className="logo">
        work<span>it</span>
      </h1>
      <a href="#">Apply for access</a>
    </header>
  );
};

export default Header;
